---
apiVersion: v1
kind: Service
metadata:
  labels:
    name: signed-commit-check-service
  name: signed-commit-check-service
spec:
  ports:
  - name: https
    port: 443
    targetPort: https
  selector:
    name: signed-commit-check